import { ChecklistGroup } from "@/interfaces/Checklist";

export const GROUP_LABELS: Record<ChecklistGroup, string> = {
    operations_prestream: "Operations Pre-stream",
    operations_poststream: "Operations Post-stream",
    training: "Training",
};
